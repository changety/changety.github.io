<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: octopress | Bian's blog]]></title>
  <link href="http://changety.github.io/blog/categories/octopress/atom.xml" rel="self"/>
  <link href="http://changety.github.io/"/>
  <updated>2014-10-16T01:00:28+08:00</updated>
  <id>http://changety.github.io/</id>
  <author>
    <name><![CDATA[BianYuting]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[一台mac维护多个octopress blog]]></title>
    <link href="http://changety.github.io/blog/2014/10/13/test2/"/>
    <updated>2014-10-13T22:03:06+08:00</updated>
    <id>http://changety.github.io/blog/2014/10/13/test2</id>
    <content type="html"><![CDATA[<h1>前言</h1>

<h2><a href="http://changety.github.io/blog/2014/10/12/setup-octopress-github-blog/">上篇</a>介绍了如何在mac下部署octopress，可能少有人会跟我一样折腾，还打算在一台mac上维护两个octopress blog，这里介绍一下具体办法，一言以蔽之就是在其他目录下再创建一个octopress，以后在此目录下维护第二个blog，以下是具体流程。</h2>

<h1>创建第二个octopress blog的repo</h1>

<h2>github不支持同一个账户创建多个github page的repo， 因此再去申请另一个github帐号就可以了，然后依然是首先到<a href="https://github.com/new">github</a>创建一个<code>anotherUsername.github.io</code>的repo，<code>anotherUsername.github.io</code>以后就是第二个blog的域名</h2>

<h1>准备安装Octopress所需的环境</h1>

<p>由于有了第一个octopress blog的安装基础， 因此默认认为mac上已经有了octopress的环境：</p>

<pre><code>$ git --version
</code></pre>

<p>git的版本号对安装octopress影响不大，这里不用关心，只要有git工具就可以了。</p>

<pre><code>$ ruby --version #此时应该已是1.9.3p125 (2012-02-16 revision 34643)，或者其他你成功安装了octopress的ruby版本
</code></pre>

<h1>安装第二个Octopress</h1>

<p>一般第一个octopress一般都装在 <code>cd ~/octopress</code> 下，这里假设把第二个octopress安装在 <code>~/anotherBlog/octopress</code>下</p>

<pre><code>$ cd ~
$ mkdir anotherBlog #作为第二个octopress所在目录
$ cd anotherBlog
$ git clone git://github.com/imathis/octopress.git octopress 
</code></pre>

<h3>同样安装完依赖跟默认主题</h3>

<p>首先查看一下<code>~/anotherBlog/octopress</code>下的ruby版本：</p>

<pre><code>$ruby --version #如果跟第一个octopress所在目录下version版本一致即可，不一样的话，参考第一篇中介绍rbenv 环境变量的设置
</code></pre>

<pre><code>$ gem install bundler
$ rbenv rehash
$ bundle install

$ rake install #默认主题
</code></pre>

<hr />

<p>运行以下命令，仔细看提示完成github和Octopress的关联（就是第一步创建的第二个博客的repo <a href="https://github.com/anotherUsername/anotherUsername.github.io">https://github.com/anotherUsername/anotherUsername.github.io</a></p>

<pre><code>$ rake setup_github_pages
</code></pre>

<p>关联成功后可以看下：</p>

<pre><code>$ git remote -v #关联的远程repo信息
</code></pre>

<p>正确关联的话就会如下显示：
<code>
octopress   git://github.com/imathis/octopress.git (fetch)
octopress   git://github.com/imathis/octopress.git (push)
origin  https://github.com/anotherUsername/anotherUsername.github.io (fetch)
origin  https://github.com/anotherUsername/anotherUsername.github.io (push)
</code></p>

<h3>接下来生成blog,本地preview一下第二个blig吧</h3>

<pre><code>$ rake generate

$ rake preview #http://localhost：4000
</code></pre>

<p>打开<a href="http://localhost:4000">http://localhost:4000/</a>，就能看到第二个octopress 也建起来了。</p>

<h2>至此第二个octopress blog 就搭建完了，就可以在一台mac下同时维护多个blog了，之后写blog、装插件、换配置就都一样了。够折腾吧- -!</h2>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[一台mac如何维护两个octopress blog]]></title>
    <link href="http://changety.github.io/blog/2014/10/13/muti-octopress-blog-on-one-pc:mac/"/>
    <updated>2014-10-13T22:03:06+08:00</updated>
    <id>http://changety.github.io/blog/2014/10/13/muti-octopress-blog-on-one-pc:mac</id>
    <content type="html"><![CDATA[<h1>前言</h1>

<h2>上篇介绍了如何在mac下部署octopress，可能少有人会跟我一样折腾，还打算在一台mac上维护两个octopress blog，这里介绍一下具体办法，一言以蔽之就是在其他目录下再创建一个octopress，以后在目录下维护第二个blog，以下是具体流程。</h2>

<h1>创建第二个octopress blog的repo</h1>

<h2>github不支持同一个账户创建多个两个github page的repo， 因此再去申请一个github帐号就可以了，然后依然是首先到<a href="https://github.com/new">github</a>创建一个<code>username.github.io</code>的repo，<code>username.github.io</code>以后就是blog的域名（当然他支持使用自定义的域名）， username就是你在github上的username。</h2>

<h1>准备安装Octopress所需的环境</h1>

<p>由于有了第一个octopress blog的安装基础， 因此默认认为mac上已经有了octopress的环境：</p>

<pre><code>$ git --version
</code></pre>

<p>git的版本号对安装octopress影响不大，这里不用关心，只要有git工具就可以了。</p>

<pre><code>$ ruby --version #此时应该已是1.9.3p125 (2012-02-16 revision 34643)，或者其他你成功安装了octopress的ruby版本
</code></pre>

<h1>安装第二个Octopress</h1>

<p>一般第一个octopress一般都装在 <code>cd ~/octopress</code> 下，这里假设把第二个octopress安装在 <code>~/secondBlog/octopress</code>下</p>

<pre><code>$ cd ~
$ mkdir secondBlog #作为第二个octopress所在目录
$ cd secondBlog
$ git clone git://github.com/imathis/octopress.git octopress 
</code></pre>

<h3>同样安装完依赖跟默认主题</h3>

<p>首先查看一下<code>~/secondBlog/octopress</code>下的ruby版本：</p>

<pre><code>$ruby --version #如果跟第一个octopress所在目录下version版本一致即可，不一样的话，参考第一篇中介绍rbenv 环境变量的设置
</code></pre>

<pre><code>$ gem install bundler
$ rbenv rehash
$ bundle install

$ rake install #默认主题
</code></pre>

<hr />

<p>运行以下命令，仔细看提示完成github和Octopress的关联（就是第一步创建的第二个博客的repo <a href="https://github.com/username/username.github.io">https://github.com/username/username.github.io</a></p>

<pre><code>$ rake setup_github_pages
</code></pre>

<p>关联成功后可以看下：</p>

<pre><code>$ git remote -v #关联的远程repo信息
</code></pre>

<p>正确关联的话就会如下显示：
<code>
octopress   git://github.com/imathis/octopress.git (fetch)
octopress   git://github.com/imathis/octopress.git (push)
origin  https://github.com/secondUsername/secondUsername.github.io (fetch)
origin  https://github.com/secondUsername/secondUsername.github.io (push)
</code></p>

<h3>接下来生成blog,本地preview一下第二个blig吧</h3>

<pre><code>$ rake generate

$ rake preview #http://localhost：4000
</code></pre>

<p>打开<a href="http://localhost%EF%BC%9A4000">http://localhost：4000</a>，就能看到第二个octopress 也建起来了。</p>

<h2>至此第二个octopress blog 就搭建完了，就可以在一台mac下同时维护两个blog了，之后写blog、装插件、换配置就都一样了，够折腾吧- -!</h2>

<p>以上便是创建一个github的全部过程了，下一篇会继续说明如果在一台电脑上管理多个octpress blog 以及 一个octopress在多台电脑上共同维护的方法。</p>

<hr />
]]></content>
  </entry>
  
</feed>
